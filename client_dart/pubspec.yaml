name: offline_first_sync_engine
description: Dart client for local SQLite mutation tracking and background syncing.
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  http: ^1.2.0
  sqflite_common_ffi: ^2.3.0
  uuid: ^4.3.3